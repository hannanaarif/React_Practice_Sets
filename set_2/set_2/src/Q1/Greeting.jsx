/*
Create a React component called Greeting that receives a name prop and displays a message
like "Hello, name!" on the DOM.
*/

function Greeting({name}){
    return <> Hello {name}</>
}

export default Greeting;